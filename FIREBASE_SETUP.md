# Настройка Firebase для проекта

## Шаг 1: Создание проекта Firebase

1. **Перейдите на** https://console.firebase.google.com/

2. **Нажмите "Добавить проект"** (Add project)

3. **Введите название проекта**
   - Например: `russian-orthography-course`

4. **Google Analytics** (опционально)
   - Можете отключить для упрощения

5. **Нажмите "Создать проект"**
   - Подождите 1-2 минуты

## Шаг 2: Добавление веб-приложения

1. **Найдите раздел добавления приложения:**
   
   **Вариант А:** На странице обзора проекта (Overview):
   - Вы увидите карточки с текстом "Начните с добавления Firebase в приложение"
   - Нажмите на иконку `</>` с подписью "Web" или "Веб"
   
   **Вариант Б:** Если не видите карточки:
   - Нажмите на **шестеренку ⚙️** (Settings) в левом верхнем углу рядом с "Project Overview"
   - Выберите **"Project settings"** (Настройки проекта)
   - Прокрутите вниз до раздела **"Your apps"** (Ваши приложения)
   - Нажмите на иконку `</>` (Web)

2. **Зарегистрируйте приложение:**
   - Введите название: `Russian Course Web App`
   - ✅ Отметьте "Firebase Hosting" (опционально)
   - Нажмите "Зарегистрировать приложение"

3. **Скопируйте конфигурацию:**
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSy...",
     authDomain: "your-project.firebaseapp.com",
     projectId: "your-project-id",
     storageBucket: "your-project.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456:web:abc123"
   };
   ```
   
   > **📝 Важно!** Сохраните эти данные - они понадобятся на Шаге 6.
   > 
   > Эту конфигурацию нужно будет вставить в файл:
   > ```
   > D:\Work\Rus\src\firebase\config.js
   > ```
   > 
   > Замените строки с `YOUR_API_KEY`, `YOUR_PROJECT_ID` и т.д. на значения из Firebase Console.

## Шаг 3: Настройка Authentication

1. **В меню слева найдите раздел "Build"** (Сборка/Создание):
   - Раскройте меню, если оно свернуто
   - Нажмите на **"Authentication"**

2. **Нажмите "Get started"**

3. **Включите методы входа:**

   **Email/Password:**
   - Нажмите на "Email/Password"
   - Переключите "Enable"
   - Сохраните

   **Google (опционально):**
   - Нажмите на "Google"
   - Переключите "Enable"
   - Выберите support email
   - Сохраните

## Шаг 4: Настройка разделе "Build" нажмите** на **"Firestore Database"**

1. **В меню слева выберите** Build → Firestore Database

2. **Нажмите "Create database"**

3. **Выберите издание базы данных:**
   - **Standard Edition** ← Выберите это (бесплатная версия)
   - Enterprise Edition (платная, для корпоративных проектов)

4. **Выберите регион (Location):**
   - Выберите ближайший: `europe-west` или `europe-central2`
   - Нажмите "Next"

5. **Выберите режим безопасности (Secure rules):**
   - **Production mode** ← Выберите это
   - Test mode (небезопасно - все могут читать/писать данные)
   - Нажмите "Create"

6. **Настройте правила безопасности:**
   
   Перейдите в раздел **Rules** и вставьте:
   ```javascript
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       // Пользователи могут читать и писать только свои данные
       match /users/{userId}/{document=**} {
         allow read, write: if request.auth != null && request.auth.uid == userId;
       }
     }
   }
   ```

7. **Нажмите "Publish"**

## Шаг 5: Создание тестового пользователя

1. **Вернитесь в раздел Authentication:**
   - Меню слева → Build → Authentication
   - Перейдите на вкладку **"Users"** (Пользователи) вверху страницы

2. **Нажмите "Add user"**

3. **Создайте демо-пользователя:**
   - Email: `demo@example.com`
   - Password: `demo123456`
   - Нажмите "Add user"

## Шаг 6: Обновление конфигурации в проекте

1. **Откройте файл** `src/firebase/config.js`

2. **Замените значения** на ваши из Firebase Console:
   ```javascript
   const firebaseConfig = {
     apiKey: "YOUR_API_KEY",              // Замените
     authDomain: "YOUR_PROJECT_ID.firebaseapp.com",  // Замените
     projectId: "YOUR_PROJECT_ID",        // Замените
     storageBucket: "YOUR_PROJECT_ID.appspot.com",   // Замените
     messagingSenderId: "YOUR_SENDER_ID", // Замените
     appId: "YOUR_APP_ID"                 // Замените
   };
   ```

## Шаг 7: Установка зависимостей и запуск

### ⚠️ ВАЖНО: Сначала установите Node.js!

**Если вы еще не установили Node.js:**

1. **Скачайте Node.js:**
   - Перейдите на https://nodejs.org/
   - Скачайте **LTS версию** (рекомендуется, например 20.x.x)
   - Запустите установщик

2. **Установите Node.js:**
   - Следуйте инструкциям установщика
   - ✅ Отметьте "Automatically install necessary tools" (если спросит)
   - Дождитесь завершения установки

3. **Перезапустите PowerShell:**
   - **Закройте все окна PowerShell**
   - Откройте PowerShell заново
   - Это необходимо для загрузки переменных окружения

4. **Проверьте установку:**
   ```powershell
   node --version
   npm --version
   ```
   
   Должны увидеть версии, например:
   ```
   v20.11.0
   10.2.4
   ```

### После установки Node.js:

5. **Установите зависимости проекта:**
   ```powershell
   cd D:\Work\Rus
   npm install
   ```
   
   Подождите, пока установятся все пакеты (firebase, vite и др.)

6. **Запустите проект:**
   ```powershell
   npm run dev
   ```

7. **Откройте браузер:**
   - http://localhost:5173
   - Войдите с demo@example.com / demo123456

## Проверка работы

### 1. Проверьте Authentication:
- Войдите в приложение
- В Firebase Console → Authentication должен появиться пользователь

### 2. Проверьте Firestore:
- Выполните действия в приложении
- В Firebase Console → Firestore Database должны появиться коллекции:
  - `users/{userId}/lessonProgress`
  - `users/{userId}/exerciseResults`
  - `users/{userId}/dictionary`

### 3. Проверьте в консоли браузера (F12):

**Нажмите F12 → Console и введите:**

```javascript
// Проверка 1: Кто залогинен?
window.currentUserId

// Проверка 2: Получить все данные
await window.ProgressService.getAllProgress(window.currentUserId)

// Проверка 3: Сохранить новый урок
await window.ProgressService.saveProgress(window.currentUserId, 'lesson-02', {
  completed: true,
  score: 100
})
```

**Если все работает - увидите данные в ответе и в Firebase Console → Firestore Database**

## Структура данных в Firestore

После первого использования в Firestore появится структура:

```
users/
  {userId}/
    lessonProgress/
      lesson-01/
        - lessonId: "lesson-01"
        - completed: true
        - score: 95
        - timeSpent: 45
        - lastAccessed: timestamp
    
    exerciseResults/
      exercise-01/
        - exerciseId: "exercise-01"
        - score: 88
        - attempts: 1
        - completed: true
    
    dictionary/
      орфография/
        - word: "орфография"
        - definition: "..."
        - example: "..."
        - mastered: false
    
    stats/
      overall/
        - lessonsCompleted: 5
        - exercisesCompleted: 3
        - totalTimeSpent: 120
```

## Безопасность

### Правила Firestore уже настроены:
- ✅ Пользователи могут читать только свои данные
- ✅ Пользователи могут записывать только в свои коллекции
- ✅ Неавторизованные пользователи не имеют доступа

### Дополнительные рекомендации:
- 🔒 Не публикуйте Firebase config в открытый репозиторий (используйте `.env`)
- 🔒 Регулярно проверяйте правила безопасности
- 🔒 Включите ограничения по квотам в Firebase Console

## Устранение неполадок

### ❌ "npm : Имя "npm" не распознано"
**Причина:** Node.js не установлен или PowerShell не перезапущен после установки

**Решение:** 
1. Установите Node.js с https://nodejs.org/
2. **Обязательно закройте и откройте PowerShell заново**
3. Проверьте: `node --version` и `npm --version`

### ❌ "Firebase: Error (auth/configuration-not-found)"
**Решение:** Проверьте, что в config.js указаны правильные значения из Firebase Console

### ❌ "Missing or insufficient permissions"
**Решение:** Проверьте правила безопасности в Firestore → Rules

### ❌ "Firebase: Error (auth/network-request-failed)"
**Решение:** Проверьте подключение к интернету

### ❌ Данные не сохраняются
**Решение:** 
1. Откройте консоль браузера (F12) → Console
2. Проверьте наличие ошибок
3. Убедитесь, что пользователь авторизован

## Полезные ссылки

- 📚 [Firebase Documentation](https://firebase.google.com/docs)
- 🔥 [Firebase Console](https://console.firebase.google.com/)
- 📖 [Firestore Rules](https://firebase.google.com/docs/firestore/security/get-started)
- 🔐 [Firebase Authentication](https://firebase.google.com/docs/auth)

---

После выполнения всех шагов ваше приложение будет готово к работе! 🚀
