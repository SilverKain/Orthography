# 📘 Курс по орфографии, правописанию и пунктуации русского языка

Полный интерактивный курс с использованием Firebase для хранения прогресса обучения.

## 🚀 Быстрый старт

### 1. Настройка Firebase

**Следуйте подробной инструкции:** [FIREBASE_SETUP.md](FIREBASE_SETUP.md)

Краткая версия:
1. Создайте проект на https://console.firebase.google.com/
2. Включите Authentication (Email/Password и Google)
3. Создайте Firestore Database
4. Скопируйте конфигурацию в `src/firebase/config.js`
5. Создайте тестового пользователя: demo@example.com / demo123456

### 2. Установка Node.js

**Если Node.js не установлен:**
1. Скачайте установщик с https://nodejs.org/ (рекомендуется LTS версия)
2. Запустите установщик и следуйте инструкциям
3. После установки перезапустите PowerShell
4. Проверьте установку:
   ```powershell
   node --version
   npm --version
   ```

### 3. Установка зависимостей

```powershell
cd D:\Work\Rus
npm install
```

### 4. Запуск development-сервера

```powershell
npm run dev
```

Откроется браузер по адресу http://localhost:5173

### 5. Сборка для production

```powershell
npm run build
```

Собранные файлы появятся в папке `dist/`

## 📁 Структура проекта

```
D:\Work\Rus\
├── src/
│   ├── firebase/
│   │   ├── services/            # Сервисы Firebase
│   │   │   ├── authService.js
│   │   │   ├── progressService.js
│   │   │   ├── exerciseService.js
│   │   │   └── dictionaryService.js
│   │   ├── config.js            # Конфигурация Firebase
│   │   └── README.md            # Документация API
│   ├── utils/                   # Вспомогательные функции
│   └── main.js                  # Главный файл приложения
├── public/                      # Статические файлы
├── index.html                   # Главная страница
├── package.json                 # Зависимости проекта
├── vite.config.js              # Конфигурация Vite
├── FIREBASE_SETUP.md           # Инструкция по настройке Firebase
└── RUSSIAN_ORTHOGRAPHY_COURSE.md  # План курса

Теоретические материалы (будут добавлены):
├── theory/
│   ├── 01-orthography/         # 15 уроков по орфографии
│   └── 02-punctuation/         # 16 уроков по пунктуации
├── practice/                    # 16 практических заданий
└── examples/                    # Примеры и образцы
```

## 🔥 Firebase + Firestore

### Структура данных

```
users/
  {userId}/
    lessonProgress/      # Прогресс по урокам
    exerciseResults/     # Результаты упражнений
    dictionary/          # Личный словарь
    stats/              # Статистика обучения
```

### Использование в консоли браузера

После входа в системуоли браузера

После запуска приложения откройте консоль (F12) и попробуйте:

const userId = window.currentUserId;

await ProgressService.getAllProgress(userId);
await ExerciseService.getExerciseStats(userId);
await DictionaryService.searchWord(userId, 'орфография');
```

## 🎯 Основные функции

### AuthService
- `login(email, password)` - вход по email/паролю
- `loginWithGoogle()`userId, lessonId)` - получить прогресс урока
- `saveProgress(userId, lessonId, data)` - сохранить прогресс
- `getAllProgress(userId)` - весь прогресс пользователя
- `completeLesson(userId, lessonId, score)` - завершить урок
- `updateStudyTime(userId, lessonId, minutes)` - обновить время
- `getModuleStats(userId, lessonIds)` - статистика модуля
- `getUserStats(userId)` - общая статистика пользователя

### ExerciseService
- `getExerciseResult(userId, exerciseId)` - результат упражнения
- `saveResult(userId, exerciseId, data)` - сохранить результат
- `getAllResults(userId)` - все результаты
- `getExerciseStats(userId)` - статистика упражнений
- `getExercisesNeedingReview(userId, threshold)` - упражнения для повторения

### DictionaryService
- `addWord(userId, word, definition, example)` - добавить слово
- `getAllWords(userId)` - все слова
- `searchWord(userId, query)` - поиск слов
- `markAsMastered(userId, wordId, mastered)` - отметить как изученное
- `deleteWord(userId, wordId)` - удалить слово
- `getDictionaryStats(userId)` - статистика словаря
- `getAllResults(userId)` - все результаты
- `getExerciseStats(userId)` - статистика упражнений
- `getExercisesNeedingReview(threshold, userId)` - упражнения для повторения

### DictionaryService
- `addWord(word, definition, example, userId)` - добавить слово
- `getAllWords(userId)` - все слова
- `searchWord(query, userId)` - поиск слов
- `markAsMastered(wordId, mastered)` - отметить как изученное

## 🔧 Полезные команды

```powershell
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка production
npm run build

# Предпросмотр сборки
npm run preview

# Запуск тестов
npm test
```

## 📊 Просмотр базы данных

1. Откройте браузер (Chrome/Edge/Firefox)
2. Нажмите F12 (DevTools)
3. ПFirebase ошибки конфигурации
```
Firebase: Error (auth/configuration-not-found)
```
**Решение:** Проверьте `src/firebase/config.js` - замените все YOUR_* значения на реальные из Firebase Console

### Ошибки прав доступа
```
Missing or insufficient permissions
```
**Решение:** Проверьте правила безопасности в Firebase Console → Firestore → Rules

### Пользователь не может войти
**Решение:** 
1. Убедитесь, что Authentication включен в Firebase Console
2. Проверьте, что Email/Password метод активирован
3. Firebase Setup](FIREBASE_SETUP.md) - подробная инструкция по настройке Firebase
- [Firebase API](src/firebase/README.md) - документация API сервисов
- [Vite](https://vitejs.dev/) - сборщик и dev-сервер
- [План курса](RUSSIAN_ORTHOGRAPHY_COURSE.md) - полная структура курса
- [Firebase Console](https://console.firebase.google.com/) - управление проектом
**Решение:** Измените порт в `vite.config.js` или закройте другое приложение

### Ошибки импорта
**Решение:** Убедитесь, что в `package.json` есть `"type": "module"`

### IndexedDB не работает
**Решение:** 
- Используйте обычный режим браузера (не инкогнито)
- Проверьте настройки приватности браузера

## 📚 Документация

- [Dexie.js](https://dexie.org/) - библиотека для работы с IndexedDB
- [Vite](https://vitejs.dev/) - сборщик и dev-сервер
- [План курса](RUSSIAN_ORTHOGRAPHY_COURSE.md) - полная структура курса

## 📝 Лицензия

MIT

---

**Версия:** 1.0.0  
**Дата создания:** 28 января 2026 г.
